trigger:
  branches:
    include:
      - main
      - driftcheck
  paths:
    include:
      - src/*

pool: default

steps:
  - script: echo "pat1-b fired for $(Build.SourceBranch) $(Build.SourceVersion)"
    displayName: 'pat1-b marker'
