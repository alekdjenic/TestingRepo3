trigger:
  branches:
    include:
      - main
      - driftcheck
  paths:
    include:
      - src/*

pool:
  vmImage: default

steps:
  - script: echo "pat1-a fired for $(Build.SourceBranch) $(Build.SourceVersion)"
    displayName: 'pat1-a marker'
